<div class="mdl-grid">
  <div class="mdl-cell mdl-cell--12-col">
    <table class="table">
      <thead>
        <tr>
          <th>From</th>
          <th>To</th>
          <th>Message</th>
          <th>Created At</th>
          <th></th>
        </tr>
      </thead>
      <tbody>
        <%= for message <- @messages do %>
          <tr>
            <td><%= message.from.uuid %></td>
            <td><%= if message.to, do: message.to.uuid %></td>
            <td><%= message.body %></td>
            <td><%= message.inserted_at %></td>
            <td class="text-right">
              <%= link "Delete", to: message_path(@conn, :delete, message), method: :delete, data: [confirm: "Are you sure?"], class: "" %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>

    <%= link "New message", to: message_path(@conn, :new) %>

  </div>
</div>
